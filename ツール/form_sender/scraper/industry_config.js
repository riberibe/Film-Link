// =====================================================================
// 業種別 収集設計書 — Jarvis自律補完システム
// =====================================================================
//
// このファイルが「どの業種を、どのサイトから、どの順番で集めるか」を定義する。
//
// 動作ルール:
//   1. adapters に書いた順番で収集を試みる（上が優先）
//   2. 1つのソースで収集しきれなければ次のソースで自動補完
//   3. すべてのソースを使い切っても足りない場合は「これが上限」と報告して終了
//   4. 重複は自動でスキップされるためソースをまたいでも二重登録なし
//
// 新しい業種を追加するには:
//   1. adapters/ にアダプターファイルを追加
//   2. このファイルに新しい業種の設定を追記するだけ
// =====================================================================

const LifullKaigoAdapter   = require('./adapters/lifull_kaigo');
const MinnanoKaigoAdapter  = require('./adapters/minnano_kaigo');

const INDUSTRY_CONFIG = {

  // ----------------------------------------------------------------
  // 有料老人ホーム・介護施設
  // 補完順: LIFULL介護 → みんなの介護
  // ----------------------------------------------------------------
  '老人ホーム': {
    adapters: [
      LifullKaigoAdapter,   // 1番目: LIFULL介護（最大〜5,000件程度）
      MinnanoKaigoAdapter,  // 2番目: みんなの介護（1番目で足りなければ自動補完）
    ],
    csvSlug: 'kaigo',
    sheetName: '老人ホーム',
    description: 'LIFULL介護 → みんなの介護 の順に自動補完',
  },

  // ----------------------------------------------------------------
  // 結婚式場・ブライダル（今後アダプター追加予定）
  // ----------------------------------------------------------------
  '結婚式場': {
    adapters: [
      // WeddingParkAdapter,    // 追加予定
      // ZexyAdapter,           // 追加予定
    ],
    csvSlug: 'wedding',
    sheetName: '結婚式場',
    description: '未実装（アダプター追加待ち）',
  },

  // ----------------------------------------------------------------
  // 工務店・ハウスメーカー（今後アダプター追加予定）
  // ----------------------------------------------------------------
  '工務店': {
    adapters: [
      // SuumoAdapter,          // 追加予定
      // HomesAdapter,          // 追加予定
    ],
    csvSlug: 'koumuten',
    sheetName: '工務店',
    description: '未実装（アダプター追加待ち）',
  },

};

// エイリアス（同じ業種の別名表記に対応）
INDUSTRY_CONFIG['有料老人ホーム'] = INDUSTRY_CONFIG['老人ホーム'];
INDUSTRY_CONFIG['介護施設']       = INDUSTRY_CONFIG['老人ホーム'];
INDUSTRY_CONFIG['ブライダル']     = INDUSTRY_CONFIG['結婚式場'];
INDUSTRY_CONFIG['ハウスメーカー'] = INDUSTRY_CONFIG['工務店'];

module.exports = INDUSTRY_CONFIG;
